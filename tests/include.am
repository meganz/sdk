# applications
TESTS = tests/api_test

if BUILD_TESTS
noinst_PROGRAMS += $(TESTS)
endif

# depends on libmega
$(TESTS): $(top_builddir)/src/libmega.la

# rules
tests_api_test_SOURCES = tests/tests.cpp
tests_api_test_CXXFLAGS = -D__TEST -I $(GTEST_DIR)/include $(LIBCURL_FLAGS) $(CRYPTO_CXXFLAGS)
tests_api_test_LDADD = $(GTEST_DIR)/libgtest_main.a $(CRYPTO_LIBS) $(LIBCURL_LIBS) $(top_builddir)/src/libmega.la
