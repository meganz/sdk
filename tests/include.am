# applications
TESTS = tests/api_test

if BUILD_TEST_APPS
noinst_PROGRAMS = $(TESTS)
endif

# depends on libmega
$(TESTS): $(top_builddir)/src/libmega.la

# rules
tests_api_test_SOURCES = tests/tests.cpp
tests_api_test_CXXFLAGS = $(GTEST_FLAGS) $(LIBCURL_FLAGS) $(CRYPTO_CXXFLAGS)
tests_api_test_LDADD = $(GTEST_LIBS) $(CRYPTO_LIBS) $(LIBCURL_LIBS) $(top_builddir)/src/libmega.la
