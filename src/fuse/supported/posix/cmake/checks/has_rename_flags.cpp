#include <cstdio>
#include <cstdlib>

int main(int, char**)
{
    [[maybe_unused]] auto exchange = RENAME_EXCHANGE;
    [[maybe_unused]] auto no_replace = RENAME_NOREPLACE;

    return EXIT_SUCCESS;
}

