#ifndef MEGA_SC_RESPONSE_PROCESSOR_H
#define MEGA_SC_RESPONSE_PROCESSOR_H 1

// #include "json.h"

namespace mega
{
// class SCResponseProcessor
// {
// public:
//     m_off_t processChunk(const char* chunk, MegaClient*);
//     size_t chunkedProgress();
//     void clear();
//     void init(MegaClient* client);

// private:
//     JSON json;
//     JSONSplitter mJsonSplitter;
//     size_t mChunkedProgress = 0;
//     // filters for JSON parsing in streaming
//     std::map<std::string, std::function<bool(JSON*)>> mFilters;

// };
}

#endif